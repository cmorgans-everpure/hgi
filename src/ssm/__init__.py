"""Storage Spend Modeler - agentic storage spend research & modelling."""
from pathlib import Path
ROOT = Path(__file__).resolve().parents[2]
CONFIG = ROOT / "config"
DATA = ROOT / "data"
OUTPUT = ROOT / "output"
